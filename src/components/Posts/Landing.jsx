import React from 'react';

import { LandingContainer } from './components/styled';

function Landing({ children }) {
  return <LandingContainer>{children}</LandingContainer>;
}

export default Landing;
